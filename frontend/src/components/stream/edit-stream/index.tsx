import React, { useState } from "react";
import { IStream } from "components/stream/definitions";
type Props = {
  setModalHeight: Function;
  streamData: IStream;
};
const EditStream: React.FC<Props> = ({ setModalHeight, streamData }) => {
  return <></>;
};

export default EditStream;
